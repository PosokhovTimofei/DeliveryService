name: CI/CD Calculator

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install YC CLI
      run: |
        curl https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
        echo "$HOME/yandex-cloud/bin" >> $GITHUB_PATH

    - name: Configure YC CLI with delivery service account
      run: |
        echo '${{ secrets.YC_KEY }}' > key.json
        yc config set service-account-key key.json
        yc config set folder-id b1gvah34lgscqb451gl4

    - name: Debug Service Account Info
      run: |
        echo "=== Service Account ID from key.json ==="
        cat key.json | jq -r '.service_account_id'
        
        echo "=== Current YC CLI configuration ==="
        yc config list
        
        echo "=== Whoami ==="
        yc iam whoami

    - name: Verify YC configuration
      run: |
        yc config list
        yc iam whoami

    - name: Log in to Yandex Container Registry
      run: |
        cat key.json | docker login --username json_key --password-stdin cr.yandex

    - name: Build Docker image
      run: |
        docker build -t cr.yandex/crppuidkaulklqnfg8l7/calculator:latest ./ -f ./calculator/Dockerfile

    - name: Push Docker image
      run: |
        docker push cr.yandex/crppuidkaulklqnfg8l7/calculator:latest

    - name: Try to deploy
      run: |
        yc serverless container revision deploy \
          --container-name calculator \
          --image cr.yandex/crppuidkaulklqnfg8l7/calculator:latest \
          --memory 256m \
          --execution-timeout 30s \
          --concurrency 1

    - name: Verify deployment
      run: |
        yc serverless container revision list --container-name calculator